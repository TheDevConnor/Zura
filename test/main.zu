have x := 1150;

// example using || and && operators
if ((x > 0 && x < 100) || (x > 1000 && x < 2000)) {
    if (x > 0 && x < 100) info "x is in the range (0, 100)";
    else info "x is in the range (1000, 2000)";
}