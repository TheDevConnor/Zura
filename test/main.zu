include "std";

fn fib(n) {
  if (n <= 1) return n;
  return fib(n - 1) + fib(n - 2);
}

have start := clock();

while (have i := 0; i <= 30) : (i = ++i) {
  info fib(i);
}

info "Time taken -> " + toString(clock() - start);
