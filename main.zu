class Person {
    say_name() { info this.name; }
}

have jane := Person();
jane.name = "Jane";

have method := jane.say_name;
method(); // prints "Jane"

//! NOTE: Code does not run have not add in this and bond methods